if not game:IsLoaded() then repeat task.wait() until game:IsLoaded() end

local Players           = game:GetService("Players")
local RS                = game:GetService("ReplicatedStorage")
local RunService        = game:GetService("RunService")
local UserInputService  = game:GetService("UserInputService")
local TweenService      = game:GetService("TweenService")
local LP                = Players.LocalPlayer

local USE_GETHUI = true
local _setIdentity = setthreadidentity or setidentity or set_thread_identity
    or (syn and syn.set_thread_identity)
local function Elevate() if _setIdentity then pcall(_setIdentity, 8) end end

local MOVE_THRESHOLD = 0.1
local MORPH_SPEED    = 8.5
local FADE           = 0.15

local BG      = Color3.fromRGB(6,6,10)
local SURFACE = Color3.fromRGB(10,0,20)
local ACCENT  = Color3.fromRGB(170,0,255)
local BORDER  = Color3.fromRGB(120,0,200)
local TEXT    = Color3.fromRGB(248,244,246)
local SUBTEXT = Color3.fromRGB(170,92,150)
local ITEMHOV = Color3.fromRGB(45,10,70)
local BTNRED  = Color3.fromRGB(205,24,48)

local SUNSET_SEQUENCE = ColorSequence.new({
    ColorSequenceKeypoint.new(0.00, Color3.fromRGB(170, 0, 255)),
    ColorSequenceKeypoint.new(0.22, Color3.fromRGB(170, 0, 255)),
    ColorSequenceKeypoint.new(0.50, Color3.fromRGB(220, 50, 255)),
    ColorSequenceKeypoint.new(0.76, Color3.fromRGB(100, 0, 180)),
    ColorSequenceKeypoint.new(1.00, Color3.fromRGB(60, 0, 120)),
})

local waterGradients = {}
local animDots = {}

task.spawn(function()
    local t = 0
    while true do
        task.wait(0.06)
        t = (t + 5) % 360
        for i = #waterGradients, 1, -1 do
            local g = waterGradients[i]
            if g and g.Parent then g.Rotation = (t + i * 19) % 360
            else table.remove(waterGradients, i) end
        end
    end
end)

local function New(cls, props)
    local ok, obj = pcall(Instance.new, cls)
    if not ok or not obj then return nil end
    if props then for k, v in pairs(props) do pcall(function() obj[k] = v end) end end
    return obj
end
local function Corner(p, r)
    local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0, r or 6); c.Parent = p; return c
end
local function Stroke(p, color, thick, trans)
    local s = Instance.new("UIStroke")
    s.Color = color or ACCENT; s.Thickness = thick or 1
    s.Transparency = trans or 0
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    s.Parent = p; return s
end
local function WaterStroke(p, thick, trans)
    local s = Stroke(p, ACCENT, thick or 1.2, trans or 0.15)
    local g = Instance.new("UIGradient")
    g.Color = SUNSET_SEQUENCE
    g.Parent = s
    table.insert(waterGradients, g)
    return s, g
end

local function MakeDraggable(frame, handle)
    handle = handle or frame
    local drag, ds, sp, ai
    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            drag = true; ds = input.Position; sp = frame.Position; ai = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if not drag or not ds then return end
        if input.UserInputType == Enum.UserInputType.MouseMovement
        or input.UserInputType == Enum.UserInputType.Touch then
            local d = input.Position - ds
            frame.Position = UDim2.new(sp.X.Scale, sp.X.Offset + d.X, sp.Y.Scale, sp.Y.Offset + d.Y)
        end
    end)
    handle.InputEnded:Connect(function(i)
        if i == ai then drag = false; ds = nil; sp = nil; ai = nil end
    end)
end

local RARITY_RANK = {
    ["OG"] = 100, ["Admin"] = 90, ["Secret"] = 80, ["Brainrot God"] = 70,
    ["Mythic"] = 60, ["Legendary"] = 50, ["Epic"] = 40, ["Rare"] = 30, ["Common"] = 20,
}
local RARITY_COLOR = {
    ["OG"] = Color3.fromRGB(255, 215, 90),
    ["Admin"] = Color3.fromRGB(255, 105, 180),
    ["Secret"] = Color3.fromRGB(210, 210, 225),
    ["Brainrot God"] = Color3.fromRGB(90, 230, 235),
    ["Mythic"] = Color3.fromRGB(235, 70, 70),
    ["Legendary"] = Color3.fromRGB(245, 165, 45),
    ["Epic"] = Color3.fromRGB(170, 95, 235),
    ["Rare"] = Color3.fromRGB(80, 140, 240),
    ["Common"] = Color3.fromRGB(150, 155, 170),
}
local DEFAULT_COLOR = Color3.fromRGB(110, 110, 128)

local _LL_FANDOM_BASE = "https://stealabrainrot.fandom.com/wiki/"
local _LL_imgCache = {}
local _LL_assetCache = {}

local function _llRequestFn()
    return (syn and syn.request) or (http and http.request) or http_request or request
end
local function _llUrlEncode(s)
    return (s:gsub("[^%w%-_%.~]", function(c) return string.format("%%%02X", string.byte(c)) end))
end
local function _llWikiCandidates(displayName)
    local clean = displayName:match("^(.-)%s*%(") or displayName
    local seen, list = {}, {}
    local function add(s)
        if s and s ~= "" and not seen[s] then seen[s] = true; table.insert(list, s) end
    end
    add((clean:gsub(" ", "_")))
    add(_llUrlEncode(clean))
    add((displayName:gsub(" ", "_")))
    add(_llUrlEncode(displayName))
    return list
end
local function _llFetchBody(requestFn, url)
    for attempt = 1, 3 do
        local ok, response = pcall(function()
            return requestFn({
                Url = url, Method = "GET",
                Headers = {
                    ["User-Agent"] = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
                    ["Accept"] = "text/html,application/xhtml+xml",
                },
                Timeout = 10,
            })
        end)
        if ok and response and response.StatusCode == 200 and response.Body and response.Body ~= "" then
            return response.Body
        end
        if ok and response and response.StatusCode and response.StatusCode ~= 200 then return nil end
        if attempt < 3 then task.wait(0.5 * attempt) end
    end
    return nil
end
local function _llFetchFandomImage(displayName)
    if _LL_imgCache[displayName] ~= nil then return _LL_imgCache[displayName] or nil end
    local requestFn = _llRequestFn()
    if not requestFn then return nil end
    for _, slug in ipairs(_llWikiCandidates(displayName)) do
        local body = _llFetchBody(requestFn, _LL_FANDOM_BASE .. slug)
        if body then
            local og = body:match('property="og:image"%s+content="([^"]+)"')
                or body:match('content="([^"]+)"%s+property="og:image"')
            if og and og ~= "" then
                og = og:gsub("&amp;", "&"):gsub("&quot;", '"')
                if og:find("^https?://") then
                    _LL_imgCache[displayName] = og
                    return og
                end
            end
        end
    end
    _LL_imgCache[displayName] = false
    return nil
end
local function _llGetBrainrotAsset(displayName)
    if _LL_assetCache[displayName] ~= nil then return _LL_assetCache[displayName] or nil end
    local url = _llFetchFandomImage(displayName)
    if not url then _LL_assetCache[displayName] = false; return nil end
    local requestFn = _llRequestFn()
    if not requestFn or type(writefile) ~= "function" or type(getcustomasset) ~= "function" then
        _LL_assetCache[displayName] = url
        return url
    end
    local ok, response = pcall(function()
        return requestFn({
            Url = url, Method = "GET",
            Headers = { ["User-Agent"] = "Mozilla/5.0" },
            Timeout = 10,
        })
    end)
    if not ok or not response or response.StatusCode ~= 200 or not response.Body then
        _LL_assetCache[displayName] = false
        return nil
    end
    local cleanName = displayName:gsub("[^%w]", "_"):sub(1, 60)
    local ext = url:match("%.(%w+)[%?#]?") or "png"
    local filename = string.format("SunsetMorph_%s.%s", cleanName, ext)
    if not pcall(writefile, filename, response.Body) then
        _LL_assetCache[displayName] = false
        return nil
    end
    local assetOk, asset = pcall(getcustomasset, filename)
    if not assetOk or not asset then
        _LL_assetCache[displayName] = false
        return nil
    end
    _LL_assetCache[displayName] = asset
    return asset
end

local BrainrotAssets
pcall(function() BrainrotAssets = require(RS.Shared.BrainrotAssets) end)

local function GetModel(name)
    local m
    if BrainrotAssets and type(BrainrotAssets.getModel) == "function" then
        pcall(function()
            local r = BrainrotAssets.getModel(name)
            if r and r:IsA("Model") then m = r:Clone() end
        end)
    end
    if not m then
        local f = RS:FindFirstChild("Models")
        f = f and f:FindFirstChild("Animals")
        local t = f and f:FindFirstChild(name)
        if t then m = t:Clone() end
    end
    return m
end

local function GetAnimals()
    local data = {}
    pcall(function() data = require(RS.Datas.Animals) end)
    local seen, list = {}, {}
    local function add(name, d)
        if type(name) ~= "string" or name == "" or seen[name] then return end
        if d and d.LuckyBlock then return end
        seen[name] = true
        list[#list+1] = {
            name = name, lower = name:lower(),
            rarity = d and d.Rarity or "?",
            rank = RARITY_RANK[d and d.Rarity] or 0,
            gen = (d and tonumber(d.Generation)) or 0,
        }
    end
    for name, d in pairs(data) do if type(d) == "table" then add(name, d) end end
    pcall(function() for _, c in RS.Models.Animals:GetChildren() do add(c.Name, nil) end end)
    pcall(function() for _, c in RS.Animations.Animals:GetChildren() do add(c.Name, nil) end end)
    table.sort(list, function(a, b)
        if a.rank ~= b.rank then return a.rank > b.rank end
        if a.gen ~= b.gen then return a.gen > b.gen end
        return a.name < b.name
    end)
    return list
end

local WALK_NAMES = {"Walk","Walking","Move","Moving","Run","Running"}
local function GetAnims(name)
    local f = RS:FindFirstChild("Animations")
    f = f and f:FindFirstChild("Animals")
    f = f and f:FindFirstChild(name)
    if not f then return nil, nil end
    local idle = f:FindFirstChild("Idle")
    local walk
    for _, wn in WALK_NAMES do
        local a = f:FindFirstChild(wn)
        if a and a:IsA("Animation") then walk = a; break end
    end
    if not walk then
        for _, c in f:GetChildren() do
            if c:IsA("Animation") and c ~= idle then walk = c; break end
        end
    end
    return idle, walk
end
local function GetAnimator(model)
    local ac = model:FindFirstChildOfClass("AnimationController")
        or model:FindFirstChildWhichIsA("AnimationController", true)
        or model:FindFirstChildWhichIsA("Humanoid", true)
    if not ac then ac = Instance.new("AnimationController"); ac.Parent = model end
    local an = ac:FindFirstChildOfClass("Animator")
    if not an then an = Instance.new("Animator", ac) end
    return an
end

local ANIMALS = GetAnimals()
local current = nil
local morph   = nil
local idleTr, walkTr = nil, nil
local hidden  = {}
local conn, speedConn

local function Char() return LP.Character end
local function Hum() local c = Char(); return c and c:FindFirstChildOfClass("Humanoid") end
local function HRP() local c = Char(); return c and c:FindFirstChild("HumanoidRootPart") end

local function SetBodyHidden(on)
    local c = Char()
    if not c then return end
    if on then
        for _, d in c:GetDescendants() do
            if morph and d:IsDescendantOf(morph) then
            elseif d:IsA("BasePart") and hidden[d] == nil then
                hidden[d] = d.Transparency; d.Transparency = 1
            elseif d:IsA("Decal") and hidden[d] == nil then
                hidden[d] = d.Transparency; d.Transparency = 1
            end
        end
    else
        for inst, v in pairs(hidden) do
            pcall(function() if inst.Parent then inst.Transparency = v end end)
        end
        hidden = {}
    end
end
local function StopAnims()
    pcall(function() if idleTr then idleTr:Stop(0) end end)
    pcall(function() if walkTr then walkTr:Stop(0) end end)
    idleTr, walkTr = nil, nil
end
local function Unmorph()
    if conn then conn:Disconnect(); conn = nil end
    if speedConn then speedConn:Disconnect(); speedConn = nil end
    StopAnims()
    if morph then morph:Destroy(); morph = nil end
    SetBodyHidden(false)
    current = nil
end
local function Attach(name)
    local c, hum, hrp = Char(), Hum(), HRP()
    if not (c and hum and hrp) then return false, "character not ready" end
    local model = GetModel(name)
    if not model then return false, "no model for "..name end
    for _, p in model:GetDescendants() do
        if p:IsA("BasePart") then
            p.CanCollide, p.CanQuery, p.CanTouch = false, false, false
            p.Massless = true
            p.Anchored = false
        end
    end
    local root = model.PrimaryPart or model:FindFirstChild("RootPart")
        or model:FindFirstChildWhichIsA("BasePart", true)
    if not root then model:Destroy(); return false, "no root part" end
    SetBodyHidden(true)
    model.Parent = c
    local footY = hrp.Position.Y - hum.HipHeight - hrp.Size.Y * 0.5
    local yaw = math.atan2(-hrp.CFrame.LookVector.X, -hrp.CFrame.LookVector.Z)
    local facing = CFrame.new(hrp.Position.X, 0, hrp.Position.Z) * CFrame.Angles(0, yaw, 0)
    model:PivotTo(facing)
    local cf, size = model:GetBoundingBox()
    local lift = footY - (cf.Position.Y - size.Y * 0.5)
    model:PivotTo(model:GetPivot() + Vector3.new(0, lift, 0))
    local weld = Instance.new("WeldConstraint")
    weld.Part0 = hrp; weld.Part1 = root; weld.Parent = root
    morph, current = model, name
    task.defer(function()
        Elevate()
        if morph ~= model or not model.Parent then return end
        pcall(function()
            local idleA, walkA = GetAnims(name)
            local an = GetAnimator(model)
            if idleA then idleTr = an:LoadAnimation(idleA); idleTr.Looped = true end
            if walkA then walkTr = an:LoadAnimation(walkA); walkTr.Looped = true end
        end)
        if idleTr then idleTr:Play(0) end
    end)
    conn = RunService.Heartbeat:Connect(function()
        if not morph or not morph.Parent then return end
        local h = Hum()
        if not h then return end
        local moving = h.MoveDirection.Magnitude > MOVE_THRESHOLD
        local want = moving and walkTr or idleTr
        local other = moving and idleTr or walkTr
        if other and other.IsPlaying then other:Stop(FADE) end
        if want and not want.IsPlaying then want:Play(FADE) end
    end)
    speedConn = RunService.RenderStepped:Connect(function()
        if not morph or not morph.Parent then return end
        local h, root = Hum(), HRP()
        if not (h and root) then return end
        local dir = h.MoveDirection
        if dir.Magnitude <= 0 then return end
        local cur = root.AssemblyLinearVelocity
        local desired = Vector3.new(dir.X * MORPH_SPEED, cur.Y, dir.Z * MORPH_SPEED)
        local delta = desired - cur
        local mass = root.AssemblyMass or 1
        pcall(function() root:ApplyImpulse(Vector3.new(delta.X, 0, delta.Z) * mass) end)
    end)
    return true
end
local function Equip(name)
    Unmorph()
    return Attach(name)
end
LP.CharacterAdded:Connect(function(c)
    Elevate()
    if not current then return end
    local name = current
    c:WaitForChild("HumanoidRootPart", 10)
    task.wait(0.2)
    hidden = {}
    Equip(name)
end)

local parentGui
if USE_GETHUI and typeof(gethui) == "function" then
    local ok, hui = pcall(gethui)
    if ok then parentGui = hui end
end
parentGui = parentGui or LP:WaitForChild("PlayerGui")

local old = parentGui:FindFirstChild("SunsetBrainrotMorph")
if old then old:Destroy() end

local sg = New("ScreenGui", {
    Name = "SunsetBrainrotMorph",
    ResetOnSpawn = false, IgnoreGuiInset = true,
    ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
    DisplayOrder = 5000, Parent = parentGui,
})

local _kvScale = Instance.new("UIScale")
_kvScale.Parent = sg
local function _refreshScale()
    pcall(function()
        local cam = workspace.CurrentCamera
        if not cam then return end
        local vp = cam.ViewportSize
        local s = math.min(vp.X / 1920, vp.Y / 1080)
        _kvScale.Scale = math.clamp(s * 0.9, 0.55, 1.15)
    end)
end
_refreshScale()
pcall(function()
    workspace.CurrentCamera:GetPropertyChangedSignal("ViewportSize"):Connect(_refreshScale)
end)

local WIN_W, WIN_H = 360, 520
local HEADER_H = 40

local win = New("Frame", {
    Name = "Main",
    Size = UDim2.fromOffset(WIN_W, WIN_H),
    Position = UDim2.new(0, 24, 0.5, -WIN_H/2),
    BackgroundColor3 = BG, BackgroundTransparency = 0.05,
    BorderSizePixel = 0, Active = true, ClipsDescendants = true,
    Parent = sg,
})
Corner(win, 12); WaterStroke(win, 2)

local bgImg = New("ImageLabel", {
    Size = UDim2.new(1,0,1,0), BackgroundTransparency = 1,
    Image = "rbxassetid://135730318591850",
    ImageColor3 = Color3.fromRGB(170, 0, 255),
    ImageTransparency = 0.6,
    ScaleType = Enum.ScaleType.Crop, ZIndex = 0, Parent = win,
})
Corner(bgImg, 12)
local bgGrad = New("UIGradient", {
    Color = SUNSET_SEQUENCE, Rotation = 90,
    Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.8),
        NumberSequenceKeypoint.new(0.5, 0.9),
        NumberSequenceKeypoint.new(1, 0.8),
    }),
    Parent = bgImg,
})
task.spawn(function()
    while bgImg.Parent do
        TweenService:Create(bgGrad, TweenInfo.new(3.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), { Rotation = 135 }):Play()
        task.wait(3.5)
        if not bgImg.Parent then break end
        TweenService:Create(bgGrad, TweenInfo.new(3.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), { Rotation = 45 }):Play()
        task.wait(3.5)
    end
end)

-- ══════════════════════════════════════════════════════════
--  ЗАГОЛОВОК — увеличен
-- ══════════════════════════════════════════════════════════
local header = New("Frame", {
    Size = UDim2.new(1, 0, 0, HEADER_H),
    BackgroundColor3 = Color3.fromRGB(45, 5, 70),
    BackgroundTransparency = 0.15,
    BorderSizePixel = 0, ZIndex = 3, Parent = win,
})
Corner(header, 12)
New("Frame", {
    Size = UDim2.new(1, 0, 0.5, 0), Position = UDim2.new(0, 0, 0.5, 0),
    BackgroundColor3 = Color3.fromRGB(45, 5, 70),
    BackgroundTransparency = 0.15, BorderSizePixel = 0, Parent = header,
})

-- Мерцающие точки в шапке
for i = 1, 8 do
    local dot = New("Frame", {
        Size = UDim2.fromOffset(math.random(2,3), math.random(2,3)),
        Position = UDim2.fromScale(math.random(), math.random()),
        BackgroundColor3 = (math.random() > 0.5) and ACCENT or Color3.fromRGB(220,50,255),
        BackgroundTransparency = math.random(40, 70) / 100,
        BorderSizePixel = 0, ZIndex = 4, Parent = header,
    })
    Corner(dot, 1)
    table.insert(animDots, {
        f = dot, phase = math.random() * 6.28,
        px = dot.Position.X.Scale, py = dot.Position.Y.Scale,
        ps = math.random(8, 16) / 10,
    })
end
task.spawn(function()
    local t = 0
    while sg.Parent do
        local dt = task.wait(0.03)
        t = t + dt
        for _, d in ipairs(animDots) do
            if not d.f.Parent then continue end
            local pulse = 0.5 + 0.4 * math.sin(t * d.ps + d.phase)
            d.f.BackgroundTransparency = 0.15 + pulse * 0.65
        end
    end
end)

-- Лого с вращением (увеличенный)
local logoBox = New("Frame", {
    Size = UDim2.fromOffset(28, 28), Position = UDim2.fromOffset(8, 6),
    BackgroundColor3 = Color3.fromRGB(45, 5, 70),
    BorderSizePixel = 0, ZIndex = 5, Parent = header,
})
Corner(logoBox, 14); WaterStroke(logoBox, 1.4)
local logoImg = New("ImageLabel", {
    Size = UDim2.new(1, -2, 1, -2), Position = UDim2.fromOffset(1, 1),
    BackgroundTransparency = 1,
    Image = "rbxassetid://135730318591850",
    ScaleType = Enum.ScaleType.Fit, Parent = logoBox,
})
Corner(logoImg, 14)
task.spawn(function()
    while logoImg.Parent do
        TweenService:Create(logoImg, TweenInfo.new(3.5, Enum.EasingStyle.Linear), {
            Rotation = logoImg.Rotation + 360,
        }):Play()
        task.wait(3.5)
    end
end)

-- Название (крупнее)
local titleLbl = New("TextLabel", {
    Size = UDim2.new(1, -110, 0, 16), Position = UDim2.fromOffset(44, 6),
    BackgroundTransparency = 1, Text = "SUNSET BRAINROT MORPH",
    TextColor3 = Color3.new(1, 1, 1),
    Font = Enum.Font.GothamBlack, TextSize = 13,
    TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 5, Parent = header,
})
local titleShine = Instance.new("UIGradient", titleLbl)
titleShine.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(180, 100, 255)),
    ColorSequenceKeypoint.new(0.25, Color3.fromRGB(255,255,255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(170,0,255)),
    ColorSequenceKeypoint.new(0.75, Color3.fromRGB(255,255,255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(180, 100, 255)),
})
table.insert(waterGradients, titleShine)

-- Подзаголовок (статус)
local subtitleLbl = New("TextLabel", {
    Size = UDim2.new(1, -110, 0, 12), Position = UDim2.fromOffset(44, 22),
    BackgroundTransparency = 1, Text = "not morphed",
    TextColor3 = Color3.fromRGB(190, 130, 220),
    Font = Enum.Font.Gotham, TextSize = 10,
    TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 5, Parent = header,
})

task.spawn(function()
    while titleLbl.Parent do
        for _, a in ipairs({0.0, 0.18, 0.4, 0.18, 0.0}) do
            if not titleLbl.Parent then break end
            TweenService:Create(titleLbl, TweenInfo.new(0.5), { TextTransparency = a }):Play()
            task.wait(0.5)
        end
        task.wait(0.25)
    end
end)
task.spawn(function()
    while titleLbl.Parent do
        TweenService:Create(titleShine, TweenInfo.new(2.0, Enum.EasingStyle.Linear), { Offset = Vector2.new(0.8, 0) }):Play()
        task.wait(2.0)
        if not titleLbl.Parent then break end
        titleShine.Offset = Vector2.new(-0.8, 0)
        TweenService:Create(titleShine, TweenInfo.new(2.0, Enum.EasingStyle.Linear), { Offset = Vector2.new(0, 0) }):Play()
        task.wait(2.0)
    end
end)

-- Мин / Close
local minBtn = New("TextButton", {
    Size = UDim2.fromOffset(20, 20), Position = UDim2.new(1, -50, 0, 10),
    BackgroundColor3 = Color3.fromRGB(40, 8, 70), BackgroundTransparency = 0.25,
    Text = "−", TextColor3 = Color3.fromRGB(245,225,255),
    Font = Enum.Font.GothamBold, TextSize = 15,
    AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 6, Parent = header,
})
Corner(minBtn, 6); WaterStroke(minBtn, 1)

local closeBtn = New("TextButton", {
    Size = UDim2.fromOffset(20, 20), Position = UDim2.new(1, -28, 0, 10),
    BackgroundColor3 = BTNRED, BackgroundTransparency = 0.3,
    Text = "✕", TextColor3 = Color3.new(1,1,1),
    Font = Enum.Font.GothamBold, TextSize = 12,
    AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 6, Parent = header,
})
Corner(closeBtn, 6); WaterStroke(closeBtn, 1)
closeBtn.Activated:Connect(function()
    Elevate(); Unmorph(); sg:Destroy()
end)
closeBtn.MouseEnter:Connect(function()
    TweenService:Create(closeBtn, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(255, 60, 80) }):Play()
end)
closeBtn.MouseLeave:Connect(function()
    TweenService:Create(closeBtn, TweenInfo.new(0.2), { BackgroundColor3 = BTNRED }):Play()
end)

MakeDraggable(win, header)

local body = New("Frame", {
    Size = UDim2.new(1, 0, 1, -HEADER_H), Position = UDim2.fromOffset(0, HEADER_H),
    BackgroundTransparency = 1, BorderSizePixel = 0, ZIndex = 3, Parent = win,
})

-- ПОИСК
local searchRow = New("Frame", {
    Size = UDim2.new(1, -16, 0, 34), Position = UDim2.fromOffset(8, 8),
    BackgroundColor3 = SURFACE, BackgroundTransparency = 0.2,
    BorderSizePixel = 0, ZIndex = 4, Parent = body,
})
Corner(searchRow, 6); WaterStroke(searchRow, 1.2, 0.2)
New("TextLabel", {
    Size = UDim2.new(0, 24, 1, 0), Position = UDim2.fromOffset(8, 0),
    BackgroundTransparency = 1, Text = "🔍",
    TextColor3 = Color3.fromRGB(190,130,255),
    Font = Enum.Font.GothamBold, TextSize = 13,
    TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 5, Parent = searchRow,
})
local searchBox = New("TextBox", {
    Size = UDim2.new(1, -36, 1, 0), Position = UDim2.fromOffset(32, 0),
    BackgroundTransparency = 1, BorderSizePixel = 0,
    Text = "", PlaceholderText = "Search brainrot...",
    PlaceholderColor3 = SUBTEXT, TextColor3 = TEXT,
    Font = Enum.Font.Gotham, TextSize = 12,
    ClearTextOnFocus = false, ZIndex = 5, Parent = searchRow,
})

-- REVERT
local revertBtn = New("TextButton", {
    Size = UDim2.new(1, -16, 0, 32), Position = UDim2.fromOffset(8, 48),
    BackgroundColor3 = BTNRED, BackgroundTransparency = 0.15,
    Text = "REVERT", TextColor3 = Color3.new(1,1,1),
    Font = Enum.Font.GothamBold, TextSize = 12,
    AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 4, Parent = body,
})
Corner(revertBtn, 6); WaterStroke(revertBtn, 1.2, 0.15)

-- СПИСОК
local listFrame = New("ScrollingFrame", {
    Size = UDim2.new(1, -16, 1, -96), Position = UDim2.fromOffset(8, 86),
    BackgroundColor3 = SURFACE, BackgroundTransparency = 0.25,
    BorderSizePixel = 0, ScrollBarThickness = 4,
    ScrollBarImageColor3 = ACCENT, ScrollBarImageTransparency = 0.1,
    ScrollingDirection = Enum.ScrollingDirection.Y,
    ElasticBehavior = Enum.ElasticBehavior.Always,
    ClipsDescendants = true,
    CanvasSize = UDim2.new(0, 0, 0, 0),
    AutomaticCanvasSize = Enum.AutomaticSize.Y,
    ZIndex = 4, Parent = body,
})
Corner(listFrame, 6); WaterStroke(listFrame, 1.2, 0.2)

local listLayout = Instance.new("UIListLayout", listFrame)
listLayout.SortOrder = Enum.SortOrder.LayoutOrder
listLayout.Padding = UDim.new(0, 4)
local listPad = Instance.new("UIPadding", listFrame)
listPad.PaddingLeft = UDim.new(0, 3); listPad.PaddingRight = UDim.new(0, 3)
listPad.PaddingTop = UDim.new(0, 3); listPad.PaddingBottom = UDim.new(0, 6)

local rows = {}

local function Refresh()
    for _, r in rows do
        if r.name == current then
            TweenService:Create(r.btn, TweenInfo.new(0.25), {
                BackgroundColor3 = Color3.fromRGB(60, 20, 100),
                BackgroundTransparency = 0.1,
            }):Play()
        else
            TweenService:Create(r.btn, TweenInfo.new(0.25), {
                BackgroundColor3 = SURFACE,
                BackgroundTransparency = 0.3,
            }):Play()
        end
    end
    subtitleLbl.Text = current and ("morphed: "..current) or "not morphed"
    subtitleLbl.TextColor3 = current and Color3.fromRGB(120, 255, 140) or Color3.fromRGB(190, 130, 220)
end

-- Строим список
for i, a in ipairs(ANIMALS) do
    local rowColor = RARITY_COLOR[a.rarity] or DEFAULT_COLOR

    local b = New("TextButton", {
        Size = UDim2.new(1, -4, 0, 46),
        BackgroundColor3 = SURFACE, BackgroundTransparency = 0.3,
        BorderSizePixel = 0, AutoButtonColor = false, Text = "",
        LayoutOrder = i, ZIndex = 5, Parent = listFrame,
    })
    Corner(b, 6)
    local rs = WaterStroke(b, 1.2, 0.25)

    -- Рарити полоса
    New("Frame", {
        Size = UDim2.new(0, 3, 0.75, 0), Position = UDim2.new(0, 0, 0.125, 0),
        BackgroundColor3 = rowColor, BorderSizePixel = 0, ZIndex = 6, Parent = b,
    })

    -- ФОТО с мягким свечением
    local iconHolder = New("Frame", {
        Size = UDim2.fromOffset(38, 38), Position = UDim2.new(0, 10, 0.5, -19),
        BackgroundColor3 = Color3.fromRGB(20, 0, 35),
        BackgroundTransparency = 0.3, BorderSizePixel = 0, ZIndex = 6, Parent = b,
    })
    Corner(iconHolder, 19); Stroke(iconHolder, rowColor, 1.2, 0.4)

    local icon = New("ImageLabel", {
        Size = UDim2.new(1, -4, 1, -4), Position = UDim2.fromOffset(2, 2),
        BackgroundTransparency = 1, Image = "",
        ScaleType = Enum.ScaleType.Fit, ZIndex = 7, Parent = iconHolder,
    })

    local placeholder = New("TextLabel", {
        Size = UDim2.new(1, 0, 1, 0),
        BackgroundTransparency = 1, Text = "?",
        TextColor3 = Color3.fromRGB(150, 130, 180),
        Font = Enum.Font.GothamBold, TextSize = 16,
        TextXAlignment = Enum.TextXAlignment.Center,
        TextYAlignment = Enum.TextYAlignment.Center,
        ZIndex = 8, Parent = iconHolder,
    })

    task.spawn(function()
        local ok, asset = pcall(_llGetBrainrotAsset, a.name)
        if ok and asset and icon and icon.Parent then
            icon.Image = asset
            if placeholder and placeholder.Parent then
                TweenService:Create(placeholder, TweenInfo.new(0.3), { TextTransparency = 1 }):Play()
                task.delay(0.3, function()
                    if placeholder and placeholder.Parent then placeholder:Destroy() end
                end)
            end
        end
    end)

    -- Название
    New("TextLabel", {
        Size = UDim2.new(1, -110, 0, 15), Position = UDim2.new(0, 56, 0, 6),
        BackgroundTransparency = 1, Text = a.name,
        TextColor3 = TEXT, Font = Enum.Font.GothamBold, TextSize = 12,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextTruncate = Enum.TextTruncate.AtEnd, ZIndex = 6, Parent = b,
    })
    -- Рарити + gen
    New("TextLabel", {
        Size = UDim2.new(1, -110, 0, 13), Position = UDim2.new(0, 56, 0, 24),
        BackgroundTransparency = 1,
        Text = (a.rarity or "?") .. "  •  " .. tostring(math.floor(a.gen)) .. "/s",
        TextColor3 = rowColor, Font = Enum.Font.GothamBold, TextSize = 9,
        TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 6, Parent = b,
    })

    b.MouseEnter:Connect(function()
        if a.name ~= current then
            TweenService:Create(b, TweenInfo.new(0.15), { BackgroundColor3 = ITEMHOV, BackgroundTransparency = 0.15 }):Play()
        end
    end)
    b.MouseLeave:Connect(function()
        if a.name ~= current then
            TweenService:Create(b, TweenInfo.new(0.15), { BackgroundColor3 = SURFACE, BackgroundTransparency = 0.3 }):Play()
        end
    end)
    b.Activated:Connect(function()
        Elevate()
        subtitleLbl.Text = "loading..."
        local ok, err = Equip(a.name)
        if not ok then subtitleLbl.Text = tostring(err) end
        Refresh()
    end)

    rows[#rows+1] = {name = a.name, lower = a.lower, btn = b}
end

searchBox:GetPropertyChangedSignal("Text"):Connect(function()
    Elevate()
    local q = searchBox.Text:lower()
    for _, r in ipairs(rows) do
        r.btn.Visible = (q == "") or r.lower:find(q, 1, true) ~= nil
    end
    listFrame.CanvasPosition = Vector2.new(0, 0)
end)

revertBtn.Activated:Connect(function()
    Elevate(); Unmorph(); Refresh()
end)
revertBtn.MouseEnter:Connect(function()
    TweenService:Create(revertBtn, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(255, 60, 80) }):Play()
end)
revertBtn.MouseLeave:Connect(function()
    TweenService:Create(revertBtn, TweenInfo.new(0.2), { BackgroundColor3 = BTNRED }):Play()
end)

Refresh()

-- ══════════════════════════════════════════════════════════
--  МИНИ-БАР
-- ══════════════════════════════════════════════════════════
local MINI_W, MINI_H = WIN_W, 28
local miniBar = New("Frame", {
    Size = UDim2.fromOffset(MINI_W, MINI_H),
    Position = UDim2.fromOffset(0, 0),
    BackgroundColor3 = Color3.fromRGB(45, 5, 70),
    BackgroundTransparency = 0.15,
    BorderSizePixel = 0, ClipsDescendants = true,
    Visible = false, ZIndex = 6, Parent = win,
})
Corner(miniBar, 12); WaterStroke(miniBar, 1.6)

local miniLogoBox = New("Frame", {
    Size = UDim2.fromOffset(20,20), Position = UDim2.fromOffset(6, 4),
    BackgroundColor3 = Color3.fromRGB(45, 5, 70),
    BorderSizePixel = 0, ZIndex = 7, Parent = miniBar,
})
Corner(miniLogoBox, 10); WaterStroke(miniLogoBox, 1)
New("ImageLabel", {
    Size = UDim2.new(1, -2, 1, -2), Position = UDim2.fromOffset(1, 1),
    BackgroundTransparency = 1,
    Image = "rbxassetid://135730318591850",
    ScaleType = Enum.ScaleType.Fit, ZIndex = 8, Parent = miniLogoBox,
})

New("TextLabel", {
    Size = UDim2.new(0, 240, 1, 0), Position = UDim2.fromOffset(34, 0),
    BackgroundTransparency = 1, Text = "SUNSET BRAINROT MORPH",
    TextColor3 = TEXT, Font = Enum.Font.GothamBlack, TextSize = 11,
    TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 7, Parent = miniBar,
})

local expandBtn = New("TextButton", {
    Size = UDim2.fromOffset(18, 16), Position = UDim2.new(1, -26, 0.5, -8),
    BackgroundColor3 = Color3.fromRGB(40, 8, 70), BackgroundTransparency = 0.25,
    Text = "▸", TextColor3 = Color3.fromRGB(245,225,255),
    Font = Enum.Font.GothamBold, TextSize = 11,
    AutoButtonColor = false, BorderSizePixel = 0,
    ZIndex = 7, Parent = miniBar,
})
Corner(expandBtn, 4); WaterStroke(expandBtn, 1)

MakeDraggable(win, miniBar)

local minimized = false
local function setMinimized(state)
    if minimized == state then return end
    minimized = state
    if minimized then
        TweenService:Create(win, TweenInfo.new(0.25, Enum.EasingStyle.Quint), {
            Size = UDim2.fromOffset(MINI_W, MINI_H),
        }):Play()
        header.Visible = false
        body.Visible = false
        miniBar.Visible = true
    else
        miniBar.Visible = false
        header.Visible = true
        body.Visible = true
        TweenService:Create(win, TweenInfo.new(0.28, Enum.EasingStyle.Quint), {
            Size = UDim2.fromOffset(WIN_W, WIN_H),
        }):Play()
    end
end

minBtn.Activated:Connect(function() Elevate(); setMinimized(true) end)
expandBtn.Activated:Connect(function() Elevate(); setMinimized(false) end)

UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.RightShift then
        sg.Enabled = not sg.Enabled
    end
end)
